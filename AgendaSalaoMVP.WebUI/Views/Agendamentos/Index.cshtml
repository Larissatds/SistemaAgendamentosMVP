@model IEnumerable<AgendamentoDTO>

@{
    ViewData["Title"] = "Gerenciamento de Agendamentos";
}

<div>
    <h2>@ViewData["Title"]</h2>
</div>
<hr />
<div class="row">
    <div class="col-md-12">
        <div>
            <div class="pull-left">
                <a asp-action="Criar" class="btn btn-outline-dark">
                    <i class="bi bi-journal-plus text-body" style="font-size: 20px;"></i>
                    Novo Agendamento
                </a>
            </div>
        </div>
    </div>
</div>
<br />
<div class="panel panel-default">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.Cliente.Nome)</th>
                <th>@Html.DisplayNameFor(model => model.Servico.NomeServico)</th>
                <th>@Html.DisplayNameFor(model => model.DataHoraAgendada)</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cliente.Nome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Servico.NomeServico)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DataHoraAgendada)
                    </td>                    
                    <td>
                        <a asp-action="Editar" asp-route-id="@item.IdAgendamento" title="Edit" class="btn btn-outline-dark">
                            <span class="fas fa-edit">Editar</span>
                        </a>

                        <a asp-action="Excluir" asp-route-id="@item.IdAgendamento" title="Delete" class="btn btn-outline-dark">
                            <span class="fas fa-trash-alt">Excluir</span>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
